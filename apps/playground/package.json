{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('playground shell: no tests yet')\"",
    "dev": "vite"
  },
  "dependencies": {
    "@monitor/sdk": "workspace:*"
  }
}
